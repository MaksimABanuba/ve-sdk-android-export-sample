package com.banuba.example.exportapp.internal

/**
 * Interface defines if it is needed
 * to export sound track as a separate music file
 */
interface EnableExportAudioProvider {
    var isEnable: Boolean
}